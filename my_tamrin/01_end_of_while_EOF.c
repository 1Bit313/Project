/*
حلقه را با دستور شرط
EOF(clr+c)
متوقف می کند
*/
#include <stdio.h>

int main()
{
    char g;
/*
    while (1)
    {
        g = getchar();
        if(g == EOF)
        {
            break;
        }
//      if(g == EOF) break; be in sorat ham mishe nevesht
    }
*/

    while ( g = getchar() != EOF)
    {
        /* code */
    }


    return 0;
}
