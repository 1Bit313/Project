/*
برنامه ای بنویسید که شماره دانشجویی و معدل تعدادی
دانشجو را از ورودی خوانده و و دانشجویی که دومین معدل را دارد در خروجی نمایش دهد
*/
#include <stdio.h>
#include <conio.h>

int main()
{
    int avg1, avg2, i=0;
    long long id1, id2;
    int max=0;
    while(1)
    {
        printf("\nenter your Student number%d: ", i);
        scanf("%lld", &id1);
        printf("\nenter your avg: ");
        scanf("%d", &avg1);

        printf("\nenter your Student number: ");
        scanf("%lld", &id2);
        printf("\nenter your avg: ");
        scanf("%d", &avg2);
        if(avg1<avg2)
        {
            avg1
        }
        i+=1
    }
}
